@model IEnumerable<Trainingtask.Models.DTO.DesigDTO>


@{
    ViewData["Title"] = "Designations";
}

<div class="container-fluid">
    <div class="row mb-3">
        <div class="col">
            <h2>Designations</h2>
        </div>
        <div class="col text-end">
            <a asp-action="DesignationSave" class="btn btn-primary">
                <i class="bi bi-plus"></i> Add Designation
            </a>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>
                                <a asp-action="DesignationSave" asp-route-id="@item.Id" class="btn btn-warning btn-sm">
                                    <i class="bi bi-pencil"></i> Edit
                                </a>
                                <a asp-action="DesignationdELETE" asp-route-id="@item.Id" class="btn btn-danger btn-sm">
                                    <i class="bi bi-trash"></i> Delete
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>